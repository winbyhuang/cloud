spring:
  application:
    name: eureka-server
  cloud:
    config:
      label: master
      #      discovery:
      #        enabled: true
      #        service-id: eureka-server
      uri: http://localhost:8888/
      name: eureka-server
      profile: dev,peer1
#      discovery:
#        enabled: true
#        service-id: common